import ComingSoon from '@/components/ui/ComingSoon';

export default function RecordPage() {
  return (
    <ComingSoon
      title="RECORD"
      subtitle="Capture your compositions. Export stems, loops, and full tracks."
      accentColor="var(--color-coral-bloom)"
    />
  );
}
